public class FindStringSize {
    /**
     *  Create a string "Hello Java" and determine:
     * - Its length using `length()`.
     * - Whether it is empty using `isEmpty()`.
     */
    public void findSize(){
        String str = "Hello Java";
        System.out.println(str.length());
        System.out.println(str.isEmpty());
    }


}
